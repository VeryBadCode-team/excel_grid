import 'package:flutter/cupertino.dart';

class ExcelTextCell extends StatelessWidget {
  const ExcelTextCell({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('test');
  }
}